# v1.0.9 — Fish Audio 模型选择修复

- Fish 模型默认使用 s2.1-pro-free，与用户提供的网页设置一致；设置中可选择或输入其他模型并保存。
- 修复固定使用 s2-pro 的问题；HTTP 402 提示检查模型及 API 额度。
- 修复设置窗口字段重叠、清除密钥时遗漏 Fish Key，以及标题版本号。
- 包含下述 Fish Audio 引擎及原有配音功能。

使用：下载 SRT-Voice-Studio.exe，选择 Fish Audio 引擎，在设置中填写 API Key 和与网页一致的模型，再在声音栏填写声音模型 ID。建议先试听，再导出故事。

验证：29 项自动化测试通过。尚未使用用户的 Fish API Key 进行真实合成验证。

## 先前更新 v1.0.7 — 网络超时处理改进

- AI 识别每批最多 30 句 / 8000 字符，每次读取等待延长至 300 秒。
- 网络超时及临时 HTTP 错误自动重试，最多 3 次；状态栏显示重试和批次。
- 失败提示区分 AI 批次、TTS 和配音句号；密钥等非临时错误不重复请求。
- 保留单一画外音 WAV / MP3 导出。AI 识别前无需切换模式，成功后自动切到多角色。

Windows x64 单文件 EXE（未签名），内置 Python / FFmpeg。Edge TTS 需要联网。
超时重试可能产生额外 API 用量；正在读取的请求取消可能需等待返回或超时。

验证：28 项自动化测试通过（包含模拟读取超时、重试、取消、330 句分批识别）；EXE 启动测试通过。未调用用户的付费 AI 接口。

## 先前更新 v1.0.8 — Fish Audio TTS

- 新增 Fish Audio 引擎、API Key 设置和官方 v1/tts 调用。
- 支持为画外音及每个角色手动填写 Fish Audio reference_id。
- 提供儿童/中年/老年、男女、沙哑/慈祥等可编辑提示项；实际模型 ID 以 Fish Audio 模型页面为准。
- 保留 Edge、兼容 TTS API、单一画外音和多角色模式。
