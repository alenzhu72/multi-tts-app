# 声幕 · SRT 多角色配音（Windows）

双击 `dist/SRT-Voice-Studio.exe`，无需安装 Python 或 FFmpeg。原有网页原型保留，桌面版入口为 `desktop.py`。

## 网络超时处理（v1.0.7）

AI 人物识别无需提前切换多角色模式：识别成功后自动切换，然后勾选人物并分配声音。

AI 按最多 30 句 / 8000 字符分批，每次请求读取超时为 300 秒。网络异常及临时服务错误最多尝试 3 次，状态栏显示重试和批次；密钥错误等非临时 HTTP 错误直接提示。正在读取的请求取消可能需要等待请求返回或超时，重试等待可立即取消。识别全部成功后才应用结果。

失败提示会区分 AI 批次、TTS 和配音句号；网络中断仍可能导致失败，重试不能保证远端服务可用。请求超时后重试可能产生额外 API 用量。

## Fish Audio（v1.0.8）

「AI / 引擎设置」新增 Fish Audio。填写 Fish Audio API Key（从 fish.audio/app/api-keys 获取），将引擎切换为 Fish Audio；在“人物与声音”中为画外音或每个角色填写 Fish Audio 声音模型 ID（fish.audio 声音页面 URL 中的 `/m/<模型 ID>`）。程序调用官方 `https://api.fish.audio/v1/tts`，使用 `s2-pro` 和 MP3 输出，再按原流程合成为 MP3/WAV。

Fish Audio 的声音模型由平台和模型创建者提供，官方接口使用 `reference_id`，没有固定的“儿童男/女、中年男/女、老年男/女、沙哑、慈祥”内置枚举。界面提供这些描述作为可编辑提示项；请将实际模型 ID 粘贴到声音栏后使用。模型可手动分配给画外音或任意角色，年龄、性别和音色以模型页面及试听为准。

## 单一画外音（v1.0.6）

默认选择「单一画外音 / Single narrator」。导入 SRT 或在「输入 TXT / SRT」中粘贴文字，在「人物与声音」页选择画外音声音，然后直接「导出 / Export MP3 / WAV」。无需点击 AI 识别，也无需 AI API Key；Edge TTS 仍需联网。

此模式下全部字幕使用同一个画外音，包括已有角色标记、以前识别过或勾选了独立声音的人物。字幕内容、角色分配及独立声音设置保留，切换「多角色 / Multiple voices」即可恢复。实际声音列和字幕试听与导出保持一致。成功完成 AI 识别后自动切到多角色模式。工程会保存模式；没有模式字段的旧工程保留原多角色行为。

## 使用

v1.0.5 的「输入 TXT / SRT」窗口支持粘贴 TXT、粘贴 SRT，以及独立的「导入 TXT」「导入 SRT」按钮。自动模式通过时间轴箭头识别 SRT，也可手动指定格式。SRT 保留原时间轴，只有 TXT 使用总时长和自动分句选项。窗口文本区随尺寸伸缩并带滚动条，底部确认按钮固定显示。

v1.0.4 默认使用英文 Edge 声音 `en-US-AriaNeural`，内置 322 个声音。主界面「配音语言」可选择英语、巴西/葡萄牙葡萄牙语、西班牙语（含地区选项）、中文等，也可选择全部语言或具体地区代码。切换语言会清空搜索并替换不属于该语言的角色声音；可以再为各角色手动选声音。工程文件保存语言和声音选择，旧工程的已有声音仍保留。声音语言选择不会翻译字幕；「刷新声音」可联网更新列表。

v1.0.2 默认人物识别使用 MiniMax 国内接口 `https://api.minimax.cn/v1` 和标准模型 `MiniMax-M2.7`，配音仍使用 Edge TTS。打开「AI / 引擎设置」，只填写第一行 AI API Key 即可，其余保持默认，TTS API 设置留空。密钥不写入工程文件。v1.0.3 起，在设置窗口点击「保存并完成」后，API Key 使用 Windows DPAPI 加密保存到 `%LOCALAPPDATA%/SRT-Voice-Studio/settings.dpapi`，下次启动自动读取。同一 Windows 用户可解密，设置中可清除已保存密钥。

旧工程可能恢复旧接口设置，可点「恢复 MiniMax 国内 + Edge」切回此配置。已适配 MiniMax 思考内容分离及带 `<think>` 标签的响应；已完成模拟接口测试，未使用用户密钥进行真实付费验证。

界面主要按钮、设置与提示同时显示中文和英文。

### TXT / 文字转 SRT

点击「TXT / 文字转 SRT · Text to SRT」，导入 `.txt` 或粘贴文字。按换行和中英文句末标点分段，长句继续拆分。默认保留原文，包括标题、冒号前缀和方括号内容。只有勾选「提取人物名前缀」才提取 `John: Hello` 等标记，并延续到同一行拆出的句子；单独的 `[Music]` 或 `John:` 仍作为正文保留。

总时长可留空，默认按中文约 4 字/秒、英文约 2.5 词/秒并加入短停顿估算。也可输入秒数（如 `90`）、`MM:SS` 或 `HH:MM:SS`（可含毫秒，如 `01:30.125`）。指定后按文本长度分配，每条首尾相接，最后结束时间精确等于设定值。

点击「生成字幕」进入字幕校正页，再点击「导出 SRT」。默认导出纯文本；勾选「SRT 包含人物标记」可携带角色分配。生成后也可继续 AI 识别和配音。

设定时长只控制字幕时间轴，不会强制将 TTS 音频压缩到相同长度；未启用时间轴配音时仍连续朗读。

### SRT 配音

1. 导入 SRT（支持 UTF-8、UTF-16 BOM、GB18030）。`example.srt` 包含五名人物和叙述，可用来测试。
2. 已有 `[人物名]` 或 `人物名：` 标记会被提取；其他句子暂归画外音。
3. 打开「AI / 引擎设置」，填写兼容 `/chat/completions` 的 Base URL、模型和密钥，点击「AI 识别故事人物」。支持兼容服务及本地接口。
4. 在「人物与声音」页勾选需要独立声音的人物，例如五人中只勾选三人，其余人物的全部台词使用画外音。画外音不能关闭。
5. 为启用的人物及画外音选择声音。「刷新 Edge 声音」获取完整列表；用 `zh-`、`en-` 或声音名筛选，也可输入完整声音 ID。
6. 字幕页用 Ctrl / Shift 多选后批量改人物；双击改文本；漏识别的人物可手动添加。
7. 点击「导出 MP3 / WAV」生成整段音频。全部字幕都会输出，人物未勾选不代表删除台词。

## 引擎与时间轴

- Edge TTS：无需密钥，需要网络；失败会重试。试听用 Windows 默认播放器打开。
- 兼容 TTS API：填写支持 `/audio/speech` 的地址、模型、声音 ID 和服务要求的密钥。切换引擎会重置声音，请重新分配。
- 连续朗读：逐句衔接，不保留 SRT 空白。
- 按字幕起点：保留起点前空白；超长台词完整播放，后续句子顺延，不裁剪、不混音，不强制匹配结束时刻。
- 语速 -50% 至 +100%。WAV 为 24kHz 单声道 PCM；MP3 为 192kbps。
- 取消不会用未完成音频覆盖目标文件。网络请求可能需等超时后取消。

## AI 与工程

AI 分析故事文本，没有声纹信息；隐含对话或跨批次别名可能判断错误，需人工校正。长字幕分批处理并传递已知人物和附近上下文。缺行、重复行号或无效结果会报错，不应用半成品。再次识别可能重新分配已经人工修改的人物。

点击 AI 识别会将字幕发送给所填服务；配音会将对应文本发送给 TTS 服务。密钥加密保存在本机，不保存到工程。工程 JSON 保存字幕、人物、声音、接口地址和模型。

## 开发与打包

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-desktop.txt
.\.venv\Scripts\python.exe desktop.py
.\.venv\Scripts\python.exe -m unittest -v test_audio_core
.\build.ps1
```

独立使用 Python/Tkinter 实现，参考 [pyVideoTrans](https://github.com/jianchang512/pyvideotrans) 的多角色工作流，没有复制其源码。使用 edge-tts 和 imageio-ffmpeg；EXE 为本地未签名构建。

## 本次验证

- SRT 解析、画外音回退、AI 返回覆盖校验、模拟 AI 接口契约测试通过。
- 实际 FFmpeg 验证 WAV 时间轴、MP3 编码、取消时保留原文件。
- 实际 Edge TTS 联网生成中文 MP3 成功。
- AI 和兼容 TTS 付费接口未真实调用，需配置用户自己的凭据。
